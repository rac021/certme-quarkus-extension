# certme-quarkus-extension

```bash

    cd ../quarkus-certme-extention/         ; \
    mvn clean install                       ; \
    cd ../certme/                           ; \
    mvn clean install                       ; \
    java -jar target/certme-my-1.0-runner.jar

```
